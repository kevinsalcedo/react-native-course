import { StyleSheet } from "react-native";

export const listItemStyles = StyleSheet.create({
  listItem: {
    width: "100%",
    marginBottom: 5,
    padding: 10,
    backgroundColor: "#eee",
    alignItems: "center",
    flexDirection: "row"
  },
  placeImage: {
    marginRight: 8,
    height: 30,
    width: 30
  }
});
